import React from "react";
import { Header, Navbar } from "../components";

const Home = () => {
  return (
    <div className="bg-hero-pattern bg-cover">
      <Navbar />
      <Header />
    </div>
  );
};

export default Home;
